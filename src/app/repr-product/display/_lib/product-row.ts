type ProductRow = {
  integratedCode: string;
  integratedName: string;
  reprCode: string;
  reprName: string;
  unitCode: string;
  unitName: string;
  revisionDate: string;
  showIntegrated: boolean;
  showRepr: boolean;
};

export default ProductRow;
